# Backend-env-ansible
Ansible deployment script for backend environment

### Installed packages
* brew
* pip
* gradle
* groovy
* docker
* nvm
* tree
* ...

### Usage
```
ansible-playbook -i "localhost," -c local env_setup.yml
```
